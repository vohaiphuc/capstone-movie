import React from 'react'
import DefaultLayout from './DefaultLayout'

export default function BookingLayout({ title, body }) {
    return (
        <DefaultLayout title={title} body={body} />
    )
}
